let
    proc  
      myProc (a : Integer,
              myProc : Integer!,
              !a : Integer    <- Este es un error
              ) ~ 
        let
          var a : Integer
        in
          nil
        end
    end
in
  nil
end